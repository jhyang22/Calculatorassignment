# lv 3 - AppUpper, ArithmeticCalculator, check

+ lv 2보다 더 높은 레벨입니다.
+ 참고사항은 다음과 같습니다
  + lv 1, 2에서는 int 타입이었던 number1, 2가 double 타입으로 바뀌었습니다.
  + 이 과정에서 제네릭을 넣어주려고 하였는데 <T>를 클래스 등에 넣어준 것 외에는 활용하지 못하였습니다.
  + lv 1, 2에서 number1, 2를 입력받을 때 문자열이 오는 경우 발생하는 예외를 처리하지 못했는데 lv 3에서는 try-catch를 활용하여 처리하였습니다.
  + lv 2에서는 리스트가 자동으로 삭제되었는데 lv 3에서는 선택에 따라 삭제가 가능하게 바뀌었습니다. (이 과정에서 인덱스 값을 입력하는 number3가 추가되었습니다)
  + 람다/스트림을 활용하여 현재 결과와 저장된 결과들을 비교하여 더 높은 결과들만 출력가능하게 하였습니다
  + enum의 경우 OperatorType enum은 만들어줬으나 enum에 대한 개념이 부족하여 활용하지 못하고 주석처리하였습니다.
+ lv 1은 calculator 패키지에, lv 2는 nextlevel 패키지에 있습니다.
