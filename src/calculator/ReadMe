# lv 1 - AppWithoutClass

+ 클래스 분리 없이 작성한 레벨입니다.
+ 참고사항은 다음과 같습니다
  + number1은 첫 번째 숫자, number2는 두 번째 숫자를 의미합니다.
  + 이 단계에서는 try-catch가 구현되지 않았기 때문에 문자열을 입력할 경우 에러가 나므로 정수를 입력해주세요
  + sign은 사칙연산 기호를 의미합니다
  + 나눗셈의 경우 분모자리(number2)에 0이 입력될 경우 문구를 출력하고 사칙연산을 종료합니다.
  + answer는 계산 종료 문구를 의미합니다. y, n, exit이 가능합니다
  + number1, 2를 제외하곤 모두 잘못 입력하였을 경우 재입력이 가능합니다.
+ lv 2는 nextlevel 패키지에, lv 3는 upperlevel 패키지에 있습니다.
